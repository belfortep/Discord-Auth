

<div class="col-md-4 p-2">
    <div class="card card-body text-light" style="background-color:#121212">
    
    
        
        
        <img src="https://cdn.discordapp.com/icons/<%= guild.id %>/<%= guild.icon %>.png" alt="">
        <h2 class="h6"><%- guild.name %> </h2>
    <% if(guild.owner){ %>
        <span class="badge bg-success">owner</span>
    <% } %>

        <button data-bs-toggle="collapse" data-bs-target="<%= '#collapse' + guild.id %>" class="btn btn-primary btn-sm">Features</button>

        <div class="collapse" id="<%= 'collapse' + guild.id %>">
            <ul >

                <% guild.features.forEach((feature)=>{  %>
                    <li><%= feature %></li>
                <% }) %>
            
                </ul> 
        </div>   
    </div>
</div>